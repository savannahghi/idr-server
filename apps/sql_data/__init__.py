"""
The SQL Metadata Module.

This app defines the metadata needed by IDR Clients to extract data from SQL
databases. It also defines the metadata that IDR Clients must submit when
uploading data to the server and adds an API for exchange of that metadata
between the server and the clients.
"""
